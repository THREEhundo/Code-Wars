/*** Grasshopper - Personalized Message
 *
 * Create a function that gives a personalized message greeting. This function takes two parameters: name & owner.
 */

const greet = (n, o) => (n === o ? 'Hello boss' : 'Hello guest')
