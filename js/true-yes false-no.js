/****************************/
/*** Convert boolean values to strings 'Yes' or 'No'	5/10
/***************************/

/**
 * Complete the method that takes a boolean value and returns a 'Yes' string for true or a 'No' string for false.
 */

const boolToWord = (bool) => (bool ? 'Yes' : 'No')
